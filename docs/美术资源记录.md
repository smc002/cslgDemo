# 美术资源记录 · 2026-09-21

## 2026-09-22：v0.7 站定动作与狙击方向

内置 imagegen 产出6张原始图：shield／medic／drone各一张8帧，sniper-directional 20帧、sniper-se 4帧、sniper-upper-diagonals 8帧。全部在 public/assets/animations/source-v07；完整提示词为 prompts.json、sniper-se-prompt.json、sniper-upper-diagonals-prompt.json。加工映射见 processing.json。

三英雄动画最终仍为2D整帧播放，但普攻／技能的下肢由同一份像素合成，不是每帧AI重画；使用固定下肢mask避免脚部动作。盾牌和警棍跨越腰线，因此采用有轮廓的遮罩并肉眼检查接缝。行走／死亡沿用已有素材。

狙击手的方向以实际图像测量值记录，不以提示词角度冒充。八份源方向约为−90、−66、−45、−15、−2、34、70、90度，加左右镜像。原始N/NE/E及补充方向做了下肢锁定；正面S/SSE因枪管覆盖腿部暂保留原生成下肢，未宣称所有狙击方向都已分层。当前为离散方向近似，后续精确连续瞄准需独立武器／手臂层。


## v0.6 初始三英雄序列帧

以内置 imagegen 分别生成持盾大汉、红发医疗兵、橙衣无人机手，输入来自原九英雄图集的第一排三个角色。每张实际 1122×1402，4列×5行，真实透明；工具未输出请求的 2048×2560，保留实际尺寸记录。三张原图与完整提示词在 `public/assets/animations/starter-source/`（`shield-sheet.png`、`medic-sheet.png`、`drone-sheet.png`、`prompts.json`、`generation-review.json`）。

每套 20 个源帧：8 帧原地行走、4 帧盾击/射击/终端指挥、4 帧格挡/治疗射击/部署无人机、4 帧踉跄到侧卧。固定斜背朝向，不做镜头转动；程序按目标方向整体水平翻转。死亡末帧保持倒地，不循环站起。待机复用行走第一帧。

`scripts/build-starter-animations.mjs` 进行确定性切图与锚点归一，所有结果保存在 `public/assets/animations/shield`、`medic`、`drone`。每个目录含五张 PNG、`clips.json` 和 `slicing.json`。保留原始生成图，后续可以针对动作细节继续替换。运行预览 `/animation-preview/index.html` 与实际战斗共用 PNG/帧元数据。

僵尸此次仅修复裁切，没有重新生图。详见 `docs/僵尸切图修复.md`，对照证据在 `docs/zombie-crop-evidence`。

## v0.5 2D 场景内城

内置 imagegen 生成三张原图，全部保存至 `public/assets/city-scene/`：

| 文件 | 尺寸与用途 |
| --- | --- |
| camp-ground.png | 1024×1536，整屏石板庭院、顶部指挥部、围墙、树林及入口 |
| lumber-atlas-v05.png | 1536×1024 RGBA，六阶段建材工坊 |
| ammo-atlas-v05.png | 1536×1024 RGBA，六阶段黑金弹工厂 |

两套图集均为真实透明，采用不等分裁切避免截掉烟囱。`scripts/slice-city-scene.ps1` 输出十二张统一锚点的 `workshop-0..5.png`、`factory-0..5.png`，详细坐标见 `slices.json`。完整三项生成提示词及共同风格保存在同目录 `prompts.json`。

风格：略写实的手绘 2D 末日营地，斜俯视、左上暖光、青绿色锈蚀金属屋顶、木材及石砖、黄铜机械细节。地面与可升级建筑分层，不是把建筑直接画死在场景里。每类六个视觉阶段对应未建、1、2、3–4、5–7、8–10 级，后续需要更多阶段时可继续补图。当前指挥部是场景装饰，两座生产建筑可点击操作。

## v0.4 明亮卡通拆图与接入

用户选定 A 原型后，用内置 imagegen 一次生成无字透明 UI 图集（1254×1254），保存为 `public/assets/ui-bright/bright-ui-atlas.png`，完整提示词 `public/assets/ui-bright/prompt.txt`。它以 A 原型为风格参考，生成金色开火键、青绿导航按钮、青绿资源胶囊、金色选中导航、金色翼盾徽章、青绿头栏。真实 alpha 已检查；没有模型文字或假数字。

`scripts/slice-bright-ui.ps1` 根据用户拆图要求确定性裁切为 `fire.png`、`nav.png`、`pill.png`、`nav-selected.png`、`medallion.png`、`header.png`。四个 `nav-merged/hunt/recruit/city.png` 直接裁自 `public/ui-concepts-v03/a-bright-cartoon.png` 的图标区域，并仅去除与外边相连的底色。原图与清理后的图集均保留；不依赖临时目录。

接入：`components/bright-ui.css`、`components/gameplay-menu.tsx`、`hunt/HuntPanel.tsx`。运行截图 `docs/ui-bright-mobile.png`。导航和控件为真正的按钮；资金、级别、倍率、援军进度均动态显示。角色、僵尸和猎场背景继续使用已有游戏素材。

## v0.3 界面方向原型

内置 image_gen 生成三张独立概念图，位于 `public/ui-concepts-v03/`，比较页 `/ui-concepts-v03/index.html`。A 明亮卡通、B 军械装甲、C 暖色营地。完整提示词随附 `prompts.json`，原始输出目录 `C:/Users/hy/.codex/generated_images/01a0c3b2-e7b5-7031-8604-a6be0bd5e16f/`。

这些是选风格的静态原型，不是已经实施的 UI 截图。生成结果约 933×1685，推图缩略框比要求稍大；正式落地须使用现有手机适配、小提示框与真实货币图标，概念图数值不作为配置。当前实现截图见 `docs/ui-v03-mobile.png`、`docs/ui-v03-desktop.png`。

内置 image_gen 生成，原图保留，复制到项目后无外部文件依赖：

| 资源 | 路径 | 尺寸/用途 |
| --- | --- | --- |
| 营地庭院 | public/assets/loop/city-courtyard.png | 1024×1536；内城，左工坊、右弹药厂、后方总部 |
| 能源核心 | public/assets/loop/energy-core.png | 1254×1254 RGBA；青色机械核心、金属外壳、透明背景 |
| 黑金弹 | public/assets/loop/black-gold-ammo.png | 1254×1254 RGBA；三枚黑色弹壳、金色弹头和金环、透明背景 |

统一提示方向：末日幸存者、手绘卡通策略游戏、清晰粗轮廓、温暖日光、橄榄绿与青绿色建筑、无文字无 UI。图标保留 alpha。城市用静态插画加真实建造/领取界面，目前没有逐栋拆件或建筑施工动画。

原始生成输出位于 C:/Users/hy/.codex/generated_images/01a0c384-b311-7be1-86d4-b3d3e8326d87/，工作副本在 C:/Users/hy/Documents/Codex/2026-09-14/xi/output/generated-loop-assets/。

旧猎场地面、冰封场景、变电站、九种僵尸图集、特殊武器僵尸、运钞僵尸、装甲车等从 E:/mori/demo/public/assets/hunt 复制；清单 public/assets/hunt/art.json 保留来源。其他角色继续沿用合流素材，两位序列帧资源生成记录详见 generation-sniper.json 与 generation-warrior-v5.json。
